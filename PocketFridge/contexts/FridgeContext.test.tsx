// FridgeContext.test.tsx
// describe('FridgeProvider', () => {
//   test('should initialize with empty fridge', () => {
//     const { result } = renderHook(() => useContext(FridgeContext));
//     expect(result.current.fridgeItems).toEqual([]);
//     expect(result.current.receipts).toEqual({});
//   });

//   test('should add items to fridge', () => {
//     // Test addItemsToFridge method
//   });

//   test('should merge items with same name', () => {
//     // Test that duplicate item names increase quantity
//   });

//   test('should create receipt for new day', () => {
//     // Test receipt creation logic
//   });
// });

// NOT FUNCTIONAL RN